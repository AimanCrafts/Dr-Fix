version: "3.9"

services:
  database:
    image: mysql:8.0
    container_name: dr-fix-database
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: dr_fix
      MYSQL_USER: dr_fix_user
      MYSQL_PASSWORD: password123
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - dr-fix-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  backend:
    build:
      context: ./packages/server
      dockerfile: Dockerfile
    container_name: dr-fix-backend
    restart: unless-stopped
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      APP_URL: http://localhost:8000
      DB_CONNECTION: mysql
      DB_HOST: database
      DB_PORT: 3306
      DB_DATABASE: dr_fix
      DB_USERNAME: dr_fix_user
      DB_PASSWORD: password123
    volumes:
      - ./packages/server:/var/www/html
    depends_on:
      database:
        condition: service_healthy
    networks:
      - dr-fix-network
    ports:
      - "8000:80"

  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: dr-fix-frontend
    restart: unless-stopped
    environment:
      VITE_API_URL: http://localhost:8000/api
    ports:
      - "5173:5173"
    volumes:
      - ./client:/app
      - /app/node_modules
    depends_on:
      - backend
    networks:
      - dr-fix-network

volumes:
  db_data:

networks:
  dr-fix-network:
    driver: bridge
